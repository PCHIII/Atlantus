# Atlantus Partners

Corporate website for Atlantus Partners, built with Astro and deployed to
GitHub Pages.

## Commands

```sh
npm install
npm run dev
npm run build
```

## Deployment

Pushes to `main` build the Astro site through GitHub Actions and publish the
generated `dist/` artifact to GitHub Pages. The custom domain is preserved by
`public/CNAME`.
